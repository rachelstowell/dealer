{
  "name": "dealer",
  "version": "1.0.0",
  "description": "Simple app that deals a card when a button is clicked",
  "main": "index.js",
  "scripts": {
    "test": "jest --ci --runInBand --reporters=default --reporters=jest-junit"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^27.4.5",
    "jest-puppeteer": "^6.0.3",
    "puppeteer": "^13.0.0"
  },
  "dependencies": {
    "jest-junit": "^13.0.0"
  },
  "engines": {
    "node": "10.14.1"
  }
}
