version: 2.0
jobs:
  build:
    docker:
      - image: ubuntu
    steps:
      - checkout
      - run:
          name: The First Step
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'
            ls -l
            apt-get update
            apt-get install node
            npm install
            npm test
            