version: 2.0
jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run:
          name: Install packages
          command:
            npm install
      - run:
          name: Run test
          command: 
            npm test
            